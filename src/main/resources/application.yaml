server.port: 7001

app:
  base-url: ${BASE_URL:http://localhost:7001}
  store-max-age: 1440 # 24 hours in minutes

spring:
  servlet:
    multipart:
      max-file-size: 30MB
      max-request-size: 50MB
